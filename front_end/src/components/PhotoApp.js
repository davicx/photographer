import PhotoSearch from './PhotoSearch';

const PhotoApp = () => {
    return (
        <div className = "photo-app">
            <p> Posts </p> 
            <PhotoSearch />
        </div>
    );
}

export default PhotoApp;

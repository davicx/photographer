import { renderWithClient } from "../utils";
import PhotoSearch from '../../components/PhotoSearch';
import PhotoCard from '../../components/PhotoCard';

describe('Check Helper me here maybe dont need', () => {
    it("Validates incorrect zipcode", async () => {
        //const resultSearch = renderWithClient(<PhotoSearch />)
        /*

        */
        //const result = renderWithClient(<PhotoCard photographer = { photographer }/>)
        //expect(await result.findByText(/Lorenzo Fay/i)).toBeInTheDocument()
        //expect(await result.findByText(/Photographer Name:/i)).toBeInTheDocument 
        //const component = shallow(<PhotoCard photographer = { photographer } key={ photographer.id } />)
        expect(true).toBe(true);
    })
})

describe('Check Helper Functions', () => {
    it("Validates incorrec", () => {
        expect(true).toEqual(true);
    })
})
